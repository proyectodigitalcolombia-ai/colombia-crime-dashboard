databases:
  - name: colombia-crime-db
    plan: starter
    databaseName: colombia_crimes
    user: crime_admin

services:
  - type: web
    name: colombia-crime-api
    env: node
    plan: starter
    # Keep pnpm aligned with package.json and the version that generated pnpm-lock.yaml.
    buildCommand: npm install -g pnpm@10.26.1 && pnpm run check:pnpm-version && echo "Using pnpm version:" && pnpm --version && pnpm install && pnpm --filter @workspace/api-server run build
    startCommand: pnpm --filter @workspace/api-server run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: colombia-crime-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: GROQ_API_KEY
        sync: false

  - type: web
    name: colombia-crime-dashboard
    env: static
    plan: starter
    staticPublishPath: ./artifacts/crime-dashboard/dist/public
    buildCommand: echo "pre-built"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: "22"
